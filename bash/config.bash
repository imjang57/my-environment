#!/bin/bash

export LOCAL_BASE=${HOME}/local
export LOCAL_BIN=${LOCAL_BASE}/bin
export LOCAL_LIB=${LOCAL_BASE}/lib
export LOCAL_OPT=${LOCAL_BASE}/opt
export LOCAL_TMP=${LOCAL_BASE}/tmp

mkdir -p ${LOCAL_BASE}
mkdir -p ${LOCAL_BIN}
mkdir -p ${LOCAL_LIB}
mkdir -p ${LOCAL_OPT}
mkdir -p ${LOCAL_TMP}

